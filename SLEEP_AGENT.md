# 睡眠管家第一版

现在支持通过配置接入真实模型服务。首页和聊天页共用一个 Chat Completions 兼容接口配置，使用 HTTPS POST 调用远程模型。未配置时明确提示配置，不会自动回退到本地固定回复。保留 `LocalSummaryModel` 供开发测试使用。

## 使用方式

1. 首页右上角点击“智能体”或调节图标（配置模型），也可从聊天页点击“配置模型”。
2. 填写完整 HTTPS 地址（以 `/chat/completions` 结尾）、服务商提供的模型名称和 API Key / 访问令牌。不要只填域名或 `/v1`。
3. 点击“测试连接”。只发送测试消息与测试作息，不读取真实睡眠记录；调用可能产生少量费用。
4. 点击“保存”，随后在首页提问或生成今晚计划。

可清除配置。修改配置会清空当前对话视图，避免把前一个服务的对话继续发送给新服务。保存意味着参数通过格式校验且已存储；“测试连接”返回成功才代表已收到服务回复。

协议采用 `model`、角色化 `messages`、`stream: false`，读取 `choices[0].message.content`。该格式可参考 [DeepSeek 官方兼容接口示例](https://api-docs.deepseek.com/)，不预设厂商或模型。当前不支持 Responses、Anthropic Messages、流式输出和工具调用协议。

首页顶部的 `HomeSleepAgentCard` 可直接查看睡眠摘要、生成今晚计划或输入问题，结果在卡片内展示，无需跳转聊天页面。请求期间禁用重复提交，失败可重试。卡片在内存中保留最近对话供后续请求使用，仅展示最新一轮；离开页面或卡片移出组合后不保证保留对话。原有聊天入口仍可使用。

首页视觉已调整为“眠伴”助手页：浅紫渐变、小月亮欢迎区、我的睡眠卡片、底部固定输入栏。报告、睡眠、社区和个人页面可从左上角导航菜单进入；底部提供摘要、报告和睡眠快捷入口。首页不再显示旧五项底部导航，其他页面保留。输入栏包含系统栏及 IME 避让，内容区独立滚动。

## 数据流

`SleepAgentChatScreen → SleepAgent → SleepAgentContextSource → SleepAgentModel`

- Android 数据源从最近 100 条记录中排除 MOCK 和未完成记录，选取最近 7 次；这不是“连续 7 晚”，也不会按日期合并午睡。
- 查询每条记录的夜间摘要。缺失摘要保持空值，不用记录起止时间冒充实际睡眠时长。
- 读取用户目标及已保存计划的作息，未设置计划时使用睡眠偏好。
- 每次请求刷新上下文，并传入最近 20 条对话消息。
- 远程模型返回 `AgentReply(text)`；计划目前包含在模型文本中，提示词要求明确标注草案，不写入设置或控制设备。结构化 `BedtimePlan` 仍用于本地实现和未来扩展。
- 对话目前仅保存在当前页面内存，关闭页面或重建 Activity 后会重置。

## 接入模型

`RemoteSleepAgentModel` 已实现 `SleepAgentModel`。应用根组件根据保存的配置创建实例，同时注入首页和聊天页；未配置时使用 `UnconfiguredSleepAgentModel`。其他协议仍可以通过实现相同接口替换。

首页卡片通过 `HomeSleepAgentCard(model = yourModel)` 注入同一接口。

API Key 和配置由 Android Keystore 的 AES-GCM 密钥加密，保存在 `noBackupFilesDir`，不会随备份迁移。密钥输入使用密码遮罩，不写入源码、日志或保存的界面状态。多用户发行时应使用自己的后端保存服务密钥，App 只持有用户访问令牌。

请求只发送近期摘要、作息、目标和对话，不发送原始 EEG、设备地址或 sessionId。连接超时 15 秒、单次读取超时 60 秒、响应上限 1 MiB；禁止自动跟随重定向。取消后不更新界面，但阻塞的网络读取可能直到超时才释放。HTTP 错误和不兼容响应使用固定错误提示，不显示服务端原始错误正文。

模型应基于实际摘要回答，明确缺失信息；不得将模拟数据、默认偏好或计划草案描述为实际发生的睡眠事实。模型输出必须在适配器内校验、转换为 `AgentReply`，错误向上抛出供页面重试，不能回退为虚构的成功回复。

本地计划以保存的上床时间为基准，列出提前 30 分钟、15 分钟的准备事项，支持跨午夜；不依据生理信号调整治疗或刺激参数。

## 验证

`gradlew.bat :app:testDebugUnitTest :app:compileDebugKotlin`

`SleepAgentTest` 覆盖空记录、缺失摘要、实际数值、跨午夜计划、模型替换、上下文刷新、历史上限和错误传播。`RemoteSleepAgentModelTest` 覆盖配置验证、请求摘要与空值、Bearer POST、响应解析、连接释放、认证/限流/重定向错误、无配置和异常响应。网络测试使用假连接，不产生真实服务调用。

需要配置实际服务凭据后，在设备上进一步验证连接、配置保存/重启恢复、密钥清除和实际模型回复。编译和本地单元测试不能替代真实服务联调。
