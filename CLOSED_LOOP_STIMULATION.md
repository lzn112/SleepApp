# 声音闭环控制与刺激日志

本实现控制声音脉冲，不修改 tDCS 电刺激协议。

## 自动刺激条件

- 移除启动会话后每 2 秒 / 2.5 秒自动播放的开环任务。
- 必须有真实 BLE 数据及成功的 ONNX 分期结果。模拟设备、模型失败回退、未知分期不触发自动刺激。
- EEG 质量至少 0.70；已提供的运动值超过 0.3 或非有限值时停止。目前采集服务没有运动数据，日志保留空值，不视为已验证无运动。
- 新分期结果产生后最多有效 45 秒；信号超过 2 秒未更新由 500 ms 看门狗检查并停止。重连 / 数据中断后重新积累 5 个完整模型窗口。
- Alpha：清醒或浅睡、会话前 30 分钟（可按设置改变）、至少 200 个有效样本预热、Alpha 幅度达到现有阈值，脉冲不应期 2 秒。当前为 ALPHA_AWARE 幅度反馈。PHASE_LOCKED 模式明确不放行，尚无经验证的声学延迟补偿。
- 深睡：满足质量条件并连续观测到 DEEP 至少 120 秒，每组最多 8 个脉冲，间隔至少 500 ms，组间观察 45 秒，每会话最多 10 组。离开深睡、信号变差、断连、暂停或停止时中断。
- 智能唤醒按两个不同且连续的模型 epoch 计数，同一个 epoch 的多个数据包不会重复计数。闹钟时间按手机本地时区计算。
- 播放失败记录失败事件并暂停自动干预；结束并重新开始会话后再尝试。

## 模型延迟边界

现有模型使用 5 × 30 秒上下文预测中间 epoch，因此输出描述的分期约滞后 60 秒。
“45 秒新鲜度”检查的是距离最新推理结果产生的时间，并不消除这 60 秒延迟。
日志 metadata_json 中记录 model_context_delay_ms=60000。
当前交付的是 Alpha 幅度反馈和分期反馈控制；不是硬件级相位锁定，也不是慢波上升相位锁定。

## 日志保存和导出

- 自动刺激日志保存在应用 SQLite 的 sound_intervention_event 表，按 session_id 关联采集会话。
- 保存开始 / 停止、分组开始 / 结束、脉冲被播放 API 接受、跳过、播放失败等事件。
- 记录墙钟毫秒、单调时钟纳秒、分期及概率、EEG 质量、可用的运动和心率、增益、Alpha 幅度及估计相位、组号、脉冲号、结果和原因。
- 连续相同的跳过原因合并为一次状态变化日志，避免每秒数百条日志；每次被接受的刺激均单独记录。
- success=true 对脉冲表示播放 API 接受请求，不代表用麦克风测得声音实际开始或刺激有效。
- 单个顺序写入器处理事件；会话结束后等待此前事件写完，才导出。写入错误会提示并暂停刺激，导出不会伪装成完整成功。
- 导出包结构：manifest.json（schema_version=2）、raw.csv、stimulation_events.csv。CSV 包含事件表全部列，按单调时钟和记录 ID 排序。
- Android 10 及以上通常导出到 Downloads/SleepAgent/sleep_session_<session_id>.zip；旧版 Android 使用应用外部文件下载目录。
- 这里保存的是自动干预会话日志；会话外的设置页手动试听不属于采集日志。
- 正常结束 / 服务销毁会等待日志写入；系统强杀或设备掉电仍可能丢失尚未提交的队列事件。

## 验证

- 控制器测试使用真实控制器、注入时钟及假音频设备，覆盖稳定等待、8 × 10 次数上限、分期变化、坏信号、模拟结果、断连、断流、Alpha 阈值与不应期、暂停、播放失败和唤醒 epoch 去重。
- 顺序写入器测试覆盖最终停止事件的写入屏障和磁盘写入错误反馈。
- CSV / ZIP 测试覆盖转义、日志文件及原始数据完整保留。
- 尚需真机验证 BLE 长时间采集、声学输出延迟、后台运行和实际 Downloads 导出。
